# MedCOBE Demo

Next.js 14 기반의 웹 애플리케이션입니다.

## 📋 사전 요구사항

다음 소프트웨어가 설치되어 있어야 합니다:

- **Node.js**: 18.x 이상 (권장: 20.x LTS)
- **pnpm**: 패키지 매니저 (npm 대신 사용)

## 🔧 Node.js 설치 방법

Node.js가 설치되어 있지 않은 경우, 아래 방법 중 하나를 선택하여 설치하세요.

### 방법 1: 공식 웹사이트에서 설치 (가장 간단)

1. [Node.js 공식 웹사이트](https://nodejs.org/) 접속
2. **LTS 버전** (Long Term Support) 다운로드 클릭
3. 다운로드된 `.pkg` 파일 실행
4. 설치 마법사를 따라 진행
5. 설치 완료 후 터미널에서 확인:
   ```bash
   node --version
   npm --version
   ```

### 방법 2: Homebrew를 통한 설치 (macOS 권장)

Homebrew가 설치되어 있는 경우:

```bash
# Homebrew 업데이트
brew update

# Node.js 설치
brew install node

# 설치 확인
node --version
npm --version
```

### 방법 3: nvm을 통한 설치 (버전 관리 권장)

여러 Node.js 버전을 관리하고 싶은 경우:

```bash
# nvm 설치
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 터미널 재시작 또는 다음 명령어 실행
source ~/.zshrc

# Node.js LTS 버전 설치
nvm install --lts

# 설치된 버전 사용
nvm use --lts

# 설치 확인
node --version
npm --version
```

### Node.js 설치 확인

터미널에서 다음 명령어로 설치 여부를 확인할 수 있습니다:

```bash
node --version
```

버전이 표시되면 설치가 완료된 것입니다. (예: `v20.11.0`)

## 📦 pnpm 설치

Node.js 설치가 완료되면 pnpm을 설치합니다:

### 방법 1: npm을 통한 설치

```bash
npm install -g pnpm
```

### 방법 2: Homebrew를 통한 설치 (macOS)

```bash
brew install pnpm
```

### 설치 확인

```bash
pnpm --version
```

## 🚀 설치 및 실행 방법

### 1. 프로젝트 디렉토리로 이동

프로젝트 파일을 다운로드한 경우, 해당 디렉토리로 이동합니다:

```bash
cd MedCOBE_demo
```

### 2. 의존성 패키지 설치

```bash
pnpm install
```

> **참고**: `pnpm install`이 실패하는 경우, `npm install` 또는 `yarn install`을 사용할 수 있습니다. 단, `pnpm-lock.yaml` 파일이 있으므로 pnpm 사용을 권장합니다.

### 3. 개발 서버 실행

```bash
pnpm dev
```

개발 서버가 시작되면 브라우저에서 [http://localhost:3000](http://localhost:3000)을 열어 확인할 수 있습니다.

### 4. 프로덕션 빌드

웹에 배포하기 전에 프로덕션 빌드를 생성합니다:

```bash
pnpm build
```

빌드가 완료되면 프로덕션 서버를 실행할 수 있습니다:

```bash
pnpm start
```

## 📦 주요 스크립트

- `pnpm dev`: 개발 서버 실행 (포트 3000)
- `pnpm build`: 프로덕션 빌드 생성
- `pnpm start`: 프로덕션 서버 실행
- `pnpm lint`: 코드 린팅 실행

## 🌐 웹 배포

### Vercel 배포 (권장)

1. [Vercel](https://vercel.com)에 계정 생성 및 로그인
2. 새 프로젝트 생성
3. GitHub 저장소 연결 또는 프로젝트 업로드
4. 빌드 설정:
   - **Framework Preset**: Next.js
   - **Build Command**: `pnpm build` (또는 `npm run build`)
   - **Output Directory**: `.next`
   - **Install Command**: `pnpm install` (또는 `npm install`)
5. 배포 버튼 클릭

### 다른 플랫폼 배포

#### Netlify

1. [Netlify](https://www.netlify.com)에 로그인
2. 새 사이트 생성 → Git 저장소 연결
3. 빌드 설정:
   - **Build command**: `pnpm build`
   - **Publish directory**: `.next`
4. 환경 변수 설정 (필요한 경우)
5. 배포

#### 자체 서버

1. 서버에 Node.js 설치
2. 프로젝트 파일 업로드
3. 의존성 설치: `pnpm install`
4. 빌드: `pnpm build`
5. PM2 등 프로세스 매니저로 실행:
   ```bash
   pm2 start npm --name "medcobe" -- start
   ```

## 📁 프로젝트 구조

```
MedCOBE_demo/
├── app/                    # Next.js App Router 페이지
│   ├── ai-assistant/       # AI 어시스턴트 페이지
│   ├── dashboard/          # 대시보드 페이지
│   ├── login/              # 로그인 페이지
│   └── ...
├── components/             # React 컴포넌트
│   └── ui/                 # shadcn/ui 컴포넌트
├── hooks/                  # 커스텀 훅
├── lib/                    # 유틸리티 함수
├── public/                 # 정적 파일
├── styles/                 # 전역 스타일
├── package.json            # 프로젝트 의존성
├── next.config.mjs         # Next.js 설정
├── tsconfig.json           # TypeScript 설정
└── tailwind.config.js      # Tailwind CSS 설정
```

## 🛠️ 기술 스택

- **Framework**: Next.js 14.2.16
- **Language**: TypeScript
- **Styling**: Tailwind CSS 4.x
- **UI Components**: shadcn/ui (Radix UI 기반)
- **Package Manager**: pnpm

## ⚠️ 주의사항

- 프로젝트는 TypeScript 빌드 에러를 무시하도록 설정되어 있습니다 (`next.config.mjs` 참조)
- 이미지 최적화가 비활성화되어 있습니다 (`images.unoptimized: true`)
- ESLint 빌드 중 에러를 무시하도록 설정되어 있습니다

## 📝 환경 변수

필요한 경우 `.env.local` 파일을 생성하여 환경 변수를 설정할 수 있습니다:

```env
# 예시
NEXT_PUBLIC_API_URL=https://api.example.com
```

## 🔧 문제 해결

### Node.js가 설치되지 않음

- 위의 "Node.js 설치 방법" 섹션을 참고하여 설치하세요.
- 설치 후 터미널을 재시작하거나 `source ~/.zshrc` 명령어를 실행하세요.

### 의존성 설치 오류

```bash
# node_modules 삭제 후 재설치
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

### 포트 충돌

다른 포트에서 실행하려면:

```bash
pnpm dev -- -p 3001
```

### 빌드 오류

```bash
# 캐시 삭제 후 재빌드
rm -rf .next
pnpm build
```

### pnpm 명령어를 찾을 수 없음

```bash
# pnpm이 전역으로 설치되지 않은 경우
npm install -g pnpm

# 또는 경로 문제인 경우
export PATH="$PATH:$(npm config get prefix)/bin"
```

## 📄 라이선스

이 프로젝트는 private 프로젝트입니다.
